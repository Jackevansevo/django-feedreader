fly scale count 1 --config fly-scrapers.toml
fly scale count 1 --config fly-celery.toml
fly scale count 1 --config fly-celerybeat.toml
fly scale count 1 --config fly-flower.toml
fly scale count 1 -a feedreader-redis
fly scale count 1 -a feedreader-pg
fly scale count 1
